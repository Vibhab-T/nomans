# Requirements:

1. C++ compiler with C++23 support
2. Make

# Compile

Clone the repo and cd into it and run:
`make`
The executable will be at /build/game

# Run

`./build/game`

# Make commands

`make clean`
`make build`
`make run`
